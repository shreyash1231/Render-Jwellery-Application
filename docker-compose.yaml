version: "3.8"

services:
  backend:
    image: 1ashusaini/jwellery-backend:latest
    container_name: jwellery-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    restart: unless-stopped
